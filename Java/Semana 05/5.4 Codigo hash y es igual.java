//El METODO EQUALS compara el contenido de dos objetos (en el caso siguiente el objeto es nombre)
//podemos comparar el atributo de nombre en este caso es posible por que el objeto nombre es de tipo string
if objects.equals(this.nombre, other.nombre));

//  Compara el  objeto para saber si son iguales
if(empleado1.equals(empleado2)){
     System.out.println("Los objetos son iguales en contenido");
   }
  else{
    System.out.println("Los objetos son distintos en contenido");
